project Test is
   package Prove is
      for Proof_Switches("Ada") use (
        "--checks-as-errors=on",
        "--warnings=error"
      );
      for Proof_Switches("kitten.adb") use (
        "--prover=colibri"
      );
   end Prove;
end Test;
